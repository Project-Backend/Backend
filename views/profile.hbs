<div class="container mt-3">
  <div class="d-flex flex-column text-center align-items-center gap-2">
    <img class="rounded-circle border" src="{{currentUser.imgUrl}}" width="100" />
    <div>
      <h4>{{currentUser.username}}</h4>
      <p>{{currentUser.email}}</p>
    </div>
  </div>

  <div class="row mt-4">
    <div class="col">
      <h2>Mis próximas actividades</h2>
      {{#if eventos}}
        {{#each eventos as |evento|}}
        <div class="row mb-4">
          <div class="col">
            {{> eventocard evento=evento}}
          </div>
        </div>
        {{/each}}
      {{else}}
      <p>No tienes eventos aún. Visita <a href="/eventos/list-eventos">nuestro listado</a> para ver si alguno te gusta.</p>
      {{/if}}
    </div>

    <div class="col">
      <h2>Eventos que he organizado:</h2>
      {{#if eventos}}
        {{#each currentUser.events as |evento|}}
        <div class="row mb-4">
          <div class="col">
            {{> eventocard evento=evento}}
            <a href="/eventos/{{evento._id}}/edit" class="btn btn-primary mt-4">Editar Evento</a>
            <a href="/eventos/{{evento._id}}/delete" class="btn btn-danger mt-4">Eliminar Evento</a>
          </div>
        </div>
        {{/each}}
      {{else}}
      <p>No tienes eventos aún. Visita <a href="/eventos/list-eventos">nuestro listado</a> para ver si alguno te gusta.</p>
      {{/if}}
    </div>
  </div>
</div>
